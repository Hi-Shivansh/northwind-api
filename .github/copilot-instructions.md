# Repo conventions

- Error messages are sentence case, no trailing period.
- Every new function gets a docstring with an Args and Returns section.
- Never use `var`. Use `const` unless reassignment is required.
- Tests live next to the file they test, named `<file>.test.js`.
